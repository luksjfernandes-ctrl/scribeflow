interface:
  display_name: "Scribeflow"
  short_description: "Repo-specific patterns and workflows for scribeflow"
  default_prompt: "Use the scribeflow repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true